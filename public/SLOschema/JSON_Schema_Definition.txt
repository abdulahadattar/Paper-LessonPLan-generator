## JSON Schema Definition

The following schema describes the structure for organizing the detailed Student Learning Outcomes data extracted from the curriculum document:

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Physics_Curriculum_SLOs_Grades_IX_X",
  "type": "array",
  "description": "A comprehensive list of Student Learning Outcomes (SLOs) for the Physics Curriculum Grades IX-X, structured by section and unit, including content codes and cognitive levels.",
  "items": {
    "type": "object",
    "properties": {
      "SLO_ID": {
        "type": "string",
        "description": "Unique identifier, combining Unit number and Content code (e.g., 'U01_1.1.1')."
      },
      "Section_Number": {
        "type": "string",
        "description": "The curriculum section number (e.g., '01')."
      },
      "Section_Name": {
        "type": "string",
        "description": "The major thematic area (e.g., 'General Physics')."
      },
      "Unit_Number": {
        "type": "string",
        "description": "The specific unit number (e.g., '01')."
      },
      "Unit_Name": {
        "type": "string",
        "description": "The title of the unit (e.g., 'Physical Quantities and Measurements')."
      },
      "Content_Code": {
        "type": "string",
        "description": "The numerical content identifier associated with the SLO (e.g., '1.1.1')."
      },
      "SLO_Text": {
        "type": "string",
        "description": "The full text of the Student Learning Outcome (what students should be able to do)."
      },
      "Cognitive_Level_Code": {
        "type": "string",
        "enum": ["K", "U", "A"],
        "description": "The assigned cognitive level (K=Knowledge/Recalling, U=Understanding, A=Applying)."
      }
    },
    "required": [
      "SLO_ID",
      "Section_Number",
      "Section_Name",
      "Unit_Number",
      "Unit_Name",
      "Content_Code",
      "SLO_Text",
      "Cognitive_Level_Code"
    ]
  }
}
```

